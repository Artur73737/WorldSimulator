{
  "name": "world-simulation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:frontend": "cd frontend && npm install --legacy-peer-deps",
    "install:backend": "cd backend && pip install -r requirements.txt --quiet",
    "install:all": "npm run install:backend && npm run install:frontend",
    "start:backend": "cd backend && uvicorn src.main:app --reload --port 8000",
    "start:frontend": "cd frontend && npm run dev",
    "all": "npm run install:all && npx concurrently --kill-others --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run start:backend\" \"npm run start:frontend\"",
    "dev": "npx concurrently --kill-others --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run start:backend\" \"npm run start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
